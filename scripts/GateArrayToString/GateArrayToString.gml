// Converts a gate array to a string
// argument0: gate array
// returns: string

var arr = argument0;
var str = "";

for (var i = 0; i < array_length_1d(arr); i++;)
	str += string(arr[i]);

return str;